# YoutubeDownloader 整理計画（レビュー反映版）

作成・改訂日：2026-09-09

> 実施状況（2026-09-09更新）：資料分類・spec修正・補助スクリプト・構造図の軽量化を実施しました。Qtの収集・属性問題に対処して新アプリを利用先へ配置し、起動ログを確認しました。実際の取得・変換と画面操作の最終確認は残っています。[実施結果と残作業](docs/development/organization_result.md) を参照してください。以下は実施前に作成した計画・集計の記録です。

## 方針と計画作成時点の進捗

目的は、利用者が迷わず必要なファイルを見つけられることです。資料の更新・分類と生成物の管理を優先し、Pythonのパッケージ集約は任意の後続作業に変更します。ルートのファイル数を減らすためだけにimport・ビルドへ影響する変更を急ぎません。

現在、完了しているのは調査・構造図の作成・計画レビュー・削除候補の隔離です。**整理は未完了、容量は未回収、ルート項目数は一時的に増加しています。** 隔離は同じプロジェクト内での移動なので、移動した約43 MiBは依然としてディスクを使用しています。今回の改訂では計画書のみを更新し、削除・コード移動・spec変更・再ビルド・配置変更は行いません。

- 当初の調査：隠し項目を含むルート20項目。
- 今回の再確認：同じ集計条件で22項目。計画書・HTML2本・隔離フォルダが増え、ルートのキャッシュと `.DS_Store` がなくなった差し引きです。
- `.DS_Store` 等の生成状態によって件数は変動します。レビューでの23項目という値は現在の再確認結果とは異なります。
- 現在のbuild・dist・ルートの.appの論理サイズ合計は363,492,353 bytes（約346.65 MiB）。隔離した古いビルド等はこの集計外にも存在します。「生成物330 MB」の値はそのまま採用せず、容量回収は手動削除後に同じ条件で再計測します。

削除はユーザーが手動で行います。実施前後の数値を分け、隔離と削除、計画と完了を混同しないよう記録します。

## 1. 隔離記録の再検証と扱い

`削除候補_2026-09-09/元の配置/` を隠しファイル込みで再確認しました。

| 集計対象 | 実ファイル数 |
|---|---:|
| 古いビルド中間生成物 | 21 |
| Pythonキャッシュ（ルート5・widgets 6・utils 1） | 12 |
| 上記小計（.DS_Storeを除く） | 33 |
| Finder表示情報 .DS_Store | 5 |
| 合計 | 38 |

合計45,326,922 bytes（43.23 MiB）。38件すべてについて `移動記録.json` のサイズ・SHA-256が実ファイルと一致しました。隔離された5件の `.DS_Store` は以下の場所に存在します。

```text
元の配置/.DS_Store
元の配置/build/.DS_Store
元の配置/dist/.DS_Store
元の配置/widgets/.DS_Store
元の配置/utils/.DS_Store
```

今回確認した元の `build/.DS_Store` と `dist/.DS_Store` は存在しません。レビューにある「隔離先33件・.DS_Storeなし」と現環境の実測は異なります。そのため38件の記録を33件へ書き換えたり、確認済みのファイルを復旧記録から除外したりはしません。更新日時だけで移動の有無を断定せず、実際のパスとハッシュを根拠にします。もし別の環境・別時点で差異が出た場合は、その時点の欠落・不一致を追記してから削除を判断します。

| 元の場所 | 隔離理由・削除時の影響 |
|---|---|
| `build/YoutubeDownloader 2/` | 4月25日の古い中間生成物。現在の主要ビルドは9月8日更新。古い警告・解析ログが不要なら削除可能。過去成果物の完全再現は保証しない |
| `__pycache__/`・`widgets/__pycache__/`・`utils/__pycache__/` | Pythonが再生成するキャッシュ |
| 上記5か所の `.DS_Store` | Finder表示情報。削除すると表示設定が初期化されることがある |

Git追跡ファイルの混入はありません。ユーザーは内容確認後、隔離フォルダを手動で削除できます。古いビルドの調査情報が必要なら先に保管します。削除は資料整理と独立して先行可能です。

復旧する場合は `元の配置/` 配下から必要な項目だけ元のパスへ戻します。再生成された同名ファイルには上書きしません。JSONと案内文は隔離内容が失われるまで復旧の根拠として保持します。

## 2. 当面保持するもの

- `.git/`：変更履歴。手動整理しない。
- `.venv/`：現在の環境。場所を変えず、使用するPython・PyInstaller・yt-dlpのバージョンを記録する。
- 現行の `build/YoutubeDownloader/`、`dist/`、ルートの `.app`：比較・必要な退避・検証が終わるまで保持。
- 全ソースと設計・要件・検討資料：履歴を含めて保持。
- main.pyと既存モジュールの配置：基本案では現状維持。現行Pythonソースは12ファイル（ルート5＋widgets 6＋utils 1、__init__.pyを含む）。

## 3. 優先実施：資料の更新と分類

1. `docs/repository_structure.md` の旧GUI構成や存在しないMakefile・起動スクリプトの記載を、現行Qt構成へ修正。
2. `docs/USER_MANUAL.md` の起動方法だけでなく、画面の選択肢と動作を照合。FLAC/M4Aの案内を現行のMP3/WAVに合わせ、ソース版の更新と `.app` の再ビルドによる更新を区別する。
3. ダウンロードの仕組み2文書も実装と照合し、旧GUIの参照を更新。
4. 以下の分類へ移動。現在も有効な要件を現行資料へ残してから、初期要件・検討メモを履歴へ分離する。

| 移動先 | ファイル |
|---|---|
| `docs/user/` | USER_MANUAL.md |
| `docs/development/` | repository_structure.md、download_mechanism.md、download_mechanism_precise.md、testing.md |
| `docs/archive/` | README_LEGACY.md、requirements.md、addrequirements.md、progress_bar_animation_study.md |

READMEのリンクは、文書の移動と同時に次の4本を変更します。行番号は改訂前の目安です。

| README行 | 現在のリンク先 | 更新先 |
|---|---|---|
| 103 | docs/USER_MANUAL.md | docs/user/USER_MANUAL.md |
| 104 | docs/testing.md | docs/development/testing.md |
| 105 | docs/progress_bar_animation_study.md | docs/archive/progress_bar_animation_study.md |
| 108 | README_LEGACY.md | docs/archive/README_LEGACY.md |

レビュー時点ではdocs相互リンクは0件。移動後は上記4本に加え、追加・変更したリンクの解決も確認します。

完了条件：説明が現行実装と一致し、利用者向け資料と過去の履歴が分かれ、リンク切れがないこと。

## 4. 構造図の置き場所・容量・Git管理

以前の「HTMLをトップに置く」という希望を維持し、`folder-map.html` と `folder-map-organized.html` は当面ローカル閲覧用としてトップに残します。ただし約2.1 MBずつの依存環境・ビルド内部の詳細JSONを持つ現状を完成形にはしません。

次の資料整理と合わせて行うこと：

- `.gitignore` に `/folder-map.html` と `/folder-map-organized.html` を追加し、生成レポートの誤コミットを防ぐ（今回は計画のみ）。
- `.venv/`・build/・dist/・.app内部・.git内部・隔離フォルダの個別ファイル一覧をHTMLへ埋め込まず、フォルダの存在・役割・集計値だけ表示。コードと資料は詳細表示を維持。
- 整理後の図は「コード配置を維持する基本案」を既定にし、パッケージ集約は任意案と明示。
- スナップショット日時、除外範囲、ファイル件数とリンク件数の区別を明記。資料の移動・隔離後に再生成。

ルートをさらに減らす場合は、2図を1つのHTML内で切り替える案を優先します。docsへ移す案はトップ配置の希望と異なるため、既定にはしません。`.gitignore`だけでは見た目・容量は改善しないので、再生成による軽量化もセットで行います。

## 5. 優先実施：二重同梱の解消と更新・配置の一本化

パッケージ集約とは切り離して実施します。

### 5.1 再ビルド前に比較・退避する

- ルート版とdist版のアプリ、現行ビルド情報についてバージョン・更新日時・実行ファイル等を比較し、戻す必要のある成果物を退避する。
- 標準のdist/buildを再ビルドで更新する前にこの確認を終える。検証は専用の `--workpath` / `--distpath` を使い、既存の正常動作品を保持する。

### 5.2 specのdatasを空にする

`build/YoutubeDownloader/PYZ-00.toc` ではqt_app、theme、queue_manager、downloader、widgetsの各モジュール、utils.logging_configが既に凍結対象に含まれています。現在のspecのdatas 6エントリはPythonコードを追加でデータ同梱しています。

**6エントリすべてを削除して `datas=[]` にする方針です。新配置へのパス書き換えはしません。** Analysisによるmain.pyからのimport追跡に任せます。将来、本当に必要な非コード資産が追加されたときだけ、その資産を個別にdatasへ指定します。

変更後は専用の出力先へクリーンビルドし、TOC・バンドルを確認。必要なモジュールが凍結され、対象の.pyソースがdatas由来で重複していないこと、画面・機能が正常に動くことを確認します。過去TOCは判断材料であり、新ビルド成功の代わりにはしません。

### 5.3 yt-dlp更新から配置までを1つの手順にする

.appはyt-dlpを内蔵し、自己更新できません。配置先を変えるだけでは更新漏れが残ります。利用先を1か所に固定する変更は、`scripts/update-and-install.sh` 等の1コマンドの更新・配置スクリプトとセットにします。

スクリプトの要件：

1. 使用環境・必要ツールを確認し、同じ.venv内のyt-dlpを更新。使用バージョンを記録。
2. 一時的な専用出力先へビルド。失敗したら配置を中止し、既存アプリを保持。
3. バンドル・署名を確認。現在READMEにあるiCloud同期下での拡張属性・署名の注意も手順に反映。
4. 明示した1つの利用先（/Applicationsまたは~/Applications等）の既存アプリを退避し、新しい成果物を配置。同時起動を避け、利用先からの起動・基本操作を確認。
5. 配置失敗時は退避したアプリへ戻す。新しい版の不具合でも戻せるよう旧版の保存先を記録。

使用者は同じコマンドで更新・ビルド・配置できます。任意のインストール先へ勝手に上書きせず、決めた利用先をスクリプトとREADMEで共通化します。起動の正常性はコピー成功だけで判断しません。

利用先で稼働を確認してから、ルートの旧.app等を追加の削除候補に移します。手動削除の役割はユーザーに維持します。

## 6. 任意・後回し：youtube_downloader/への集約

**整理完了の必須条件から外します。** 現状の規模では、主な利点はルートの4モジュール＋2ディレクトリを1つへまとめること（差し引き5項目の削減）です。そのために多数のimport・spec・再ビルド検証へ影響が及ぶため、資料整理と生成物管理を先に完了させます。配布パッケージ化や規模拡大などの目的ができた場合に再検討します。

実施する場合：

- main.pyはルートに維持。qt_app.py・queue_manager.py・downloader.py・theme.py・widgets/・utils/をyoutube_downloader/へまとめる。
- ルート前提importはレビューで18箇所確認。実施時に再検索し、関数内の遅延importも漏れなく更新。
- themeからqueue_manager.ItemStatusへの参照を維持し、構造変更と機能再設計を混ぜない。
- `utils/__init__.py` は必須ではない。現状は名前空間パッケージとして動作し、utils.logging_configも凍結済み。通常パッケージへ統一する設計上の選択として追加することはできるが、動作の前提にはしない。
- specのdatasは空の方針を維持し、移動した.pyをデータとして再登録しない。
- 資料・README・構造図も移行後の配置へ再更新する。

## 7. 検証と完了条件

資料整理はリンク・実装との照合で検証します。spec変更、配置スクリプト、任意のパッケージ移行では、必要な段階ごとに以下を確認します。

- ソース版とビルド版の両方で起動。
- 単一URL追加、一括追加、キュー未追加の入力欄からの直接開始、チャンネル取得。関数内importが使われる経路も通す。
- 形式選択、開始・停止、キュー状態、進捗、保存先、ログ出力。必要な取得テストは小さな利用可能な動画で行う。
- ビルド版の更新は再ビルド・配置で反映されること。配置失敗時・不具合時の復旧手順を確認。
- Git差分に環境・生成物・隔離ファイル・大きなレポートが混入していないこと。
- 構造図が実配置と一致し、見た目の簡素化と容量の変化を同じ条件で再計測すること。

基本案の完了は「現行資料へ迷わず辿れる」「古い生成物を利用物と区別できる」「二重同梱が解消される」「アプリ更新・配置が1つの手順になる」「動作が維持される」で判断します。パッケージ集約を行わなくても完了とします。ユーザーの手動削除待ちは、整理済み・隔離中・削除済みを区別して記載します。
